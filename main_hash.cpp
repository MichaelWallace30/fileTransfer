#include <iostream>
#include <vector>
#include <string>
#include <cmath>
#include <iomanip>
#include <sstream>
#include <ctime>
#include "hash.h"
#include <string>

// Example Usage
int main(){

    /*
    std::vector<char> in = {'T', 'e', 's', 't', '1' };
    for(char c : in) std::cout << c;
    std::cout << std::endl;

    std::vector<char> out = *hash_and_append(&in);
    std::cout << "Hash = ";
    for(char c : out) std::cout << c;
    std::cout << "\n\n";


    in = {'T', 'e', 's', 't', '2' };
    for(char c : in) std::cout << c;
    std::cout << "\n";

    out = *hash_and_append(&in);
    std::cout << "Hash = ";
    for(char c : out) std::cout << c;
    std::cout << "\n\n";

    in = {'T', 'e', 's', 't', '2' };
    seed_rand();

    out = *salt_and_hash(&in);
    std::cout << "Hash = ";
    for(char c : out) std::cout << c;
    std::cout << "\n\n";

    in = {'T', 'e', 's', 't', '2' };
    out = *salt_and_hash(&in);
    std::cout << "Hash = ";
    for(char c : out) std::cout << c;
    std::cout << std::endl;
    */





    std::cout << passWord << std::endl;
    return 0;
}

